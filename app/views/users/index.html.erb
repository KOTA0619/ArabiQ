<% if @users.any? %>
  <div class="card-deck row">
    <% @users.each do |user| %>
        <div class="col-sm-3">
            <div class="mt-3 w-75">
              <%= render 'layouts/prof_icon', user: user%>
            </div>
        </div>
    <% end %>
  </div>
<% end %>
